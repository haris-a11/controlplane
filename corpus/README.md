Drop the real policy document here as `.txt` or `.pdf`.

`sample-policy.txt` is a placeholder so the prototype runs from a clean clone.
Replace it with the document used in the Round 1 demo before recording the video.
